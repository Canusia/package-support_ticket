[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "myce-support-ticket"
version = "0.0.1"
description = "MyCE - Support Tickets"
requires-python = ">=3.8"
